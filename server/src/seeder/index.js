/* for (var i = 0; i < 15; i++) {
  let fake = Gallery({
    gallery_name: faker.lorem.words(2),
    gallery_description: faker.lorem.words(20),
    photos: [
      {
        address: StarWars.jpeg,
      },
    ]
  });
  fake.save(); */
